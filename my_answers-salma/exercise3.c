#include <stdio.h>
  int main()
  {
     int n;
	 printf("please enter a number: ");
	 scanf("%i",&n);
	 if (n % 2 == 0) //  an even is an integer divisible by 2 => the remainder is 0
	 { printf("the number %i is an even\n",n);
	 }
	 else 
	 {
	 printf("the number %i is an odd\n",n);
	 }
	 return 0;













  }

